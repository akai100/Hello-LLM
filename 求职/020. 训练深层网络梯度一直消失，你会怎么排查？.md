1. 观察训练日志

   + loss 长期不下降 -> 可能梯度消失
   + 梯度范数（norm）接近 0

2. 检查梯度分布

   + 对每层梯度做统计（mean/std）
   + 是否在深层几乎为 0

3. 检查激活分布

   + 前向传播输出是否在饱和区（sigmoid/tanh 的 ±1）
   + 是否过小 → 梯度衰减

4. 检查权重初始化

  + 权重是否过小 → 方差过小 → 梯度被压缩
  + 是否全 0 → 对称性无法打破

5. 检查网路结构

   + 是否深层没有残差连接
   + 是否使用了不合适的激活函数
